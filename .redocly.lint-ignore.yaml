# Logout is intentionally idempotent: it clears the refresh cookie and returns 204 even when no session exists.
docs/openapi.yaml:
  operation-4xx-response:
    - '#/paths/~1api~1v1~1auth~1logout/post/responses'
